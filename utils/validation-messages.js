module.exports = {
  requiredValidationMessage: '{PATH} is required.',
  minLengthValidationMessage: 'The value of path `{PATH}` (`{VALUE}`) is shorter than the minimum allowed length ({MINLENGTH}).',
  maxLengthValidationMessage: 'The value of path `{PATH}` (`{VALUE}`) is longer than the maximum allowed length ({MAXLENGTH}).'
}
