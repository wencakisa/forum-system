{{#if result}}
  <h1>Latest posts: </h1>
  <ul class="list-group">
    {{#each result}}
      <li class="list-group-item"><a href="/post/{{id}}">{{title}}</a></li>
    {{/each}}
  </ul>
{{else}}
  <h1>No posts available.</h1>
{{/if}}